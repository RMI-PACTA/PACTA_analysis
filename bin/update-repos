#! /bin/bash

head=$(git rev-list HEAD -1)
git checkout master
git pull origin master

cd ../pacta-data
git checkout master
git pull origin master

cd ../create_interactive_report
git checkout master
git pull origin master

cd ../StressTestingModelDev
git checkout master
git pull origin master

cd ../PACTA_analysis
git checkout "$head"
